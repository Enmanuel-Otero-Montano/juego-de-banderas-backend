# Dependencias directas de producción. Las transitivas las resuelve pip dentro
# de las restricciones declaradas por cada proyecto.
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
email-validator==2.3.0
pydantic-settings==2.15.0

SQLAlchemy==2.0.54
alembic==1.20.0
psycopg2-binary==2.9.13

PyJWT==2.14.0
pwdlib[argon2,bcrypt]==0.3.1
# Conserva la verificación de hashes históricos; los accesos correctos se
# migran automáticamente a Argon2id.
bcrypt==4.3.0

Pillow==12.3.0
requests==2.34.2
slowapi==0.1.10
